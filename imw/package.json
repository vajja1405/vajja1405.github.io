{
  "name": "interview-my-work",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interview My Work: an evidence-gated AI workspace for the portfolio",
  "scripts": {
    "dev": "vite",
    "build": "python3 ../scripts/build_evidence.py && tsc --noEmit && vite build && python3 ../scripts/stamp_version.py",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests",
    "test": "vitest run",
    "eval": "vitest run --reporter=default --reporter=json --outputFile=../evidence/dist/.engine-eval.json && cd ../api && .venv/bin/python -m pytest -q --junitxml=../evidence/dist/.api-eval.xml && cd .. && python3 scripts/eval_report.py"
  },
  "dependencies": {
    "jspdf": "^4.2.1",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.6.2",
    "eslint": "^9.39.5",
    "globals": "^17.12.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.1",
    "vite": "^6.4.3",
    "vitest": "^4.1.11"
  }
}
